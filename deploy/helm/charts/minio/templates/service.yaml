apiVersion: v1
kind: Service
metadata:
  name: minio
  labels:
    app.kubernetes.io/name: minio
spec:
  clusterIP: None
  ports:
    - name: api
      port: 9000
    - name: console
      port: 9001
  selector:
    app.kubernetes.io/name: minio
