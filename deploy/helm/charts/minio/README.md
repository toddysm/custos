# minio

MinIO distributed object storage for Custos HA profiles. Provides buckets for artifacts, Postgres backups, and Loki long-term storage. Eval profiles do not install this — they use PVCs directly.
