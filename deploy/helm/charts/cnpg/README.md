# cnpg

CloudNativePG `Cluster` CR for the Custos Postgres database. The CNPG operator and CRDs must be installed on the cluster before this subchart renders. The operator is intentionally out-of-band so users can manage its lifecycle independently.
