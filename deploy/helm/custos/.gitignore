# Transient outputs of `helm dependency update` against the umbrella chart.
# Reconstructed from `dependencies` in Chart.yaml on every fresh checkout.
Chart.lock
charts/
# Scratch directory helm creates while rebuilding an already-populated charts/.
tmpcharts-*/
