All `<PREFIX>-NNN` tasks for `<component>` are complete. `<PREFIX>-000-<COMPONENT>` milestone delivered.

## Phases delivered

- **Phase A — `<phase name>`**: `<one-line summary>` (tasks `<PREFIX>-001` … `<PREFIX>-002`).
- **Phase B — `<phase name>`**: `<one-line summary>` (tasks `<PREFIX>-003` … `<PREFIX>-004`).
- **Phase C — `<phase name>`**: `<one-line summary>` (tasks `<PREFIX>-005` … `<PREFIX>-006`).

## Key files added

- `<path>` — `<one-line description>`.
- `<path>` — `<one-line description>`.

## Stats

- Tasks completed: `<N>`.
- PRs merged: `<N>`.
- Total commits: `<N>`.
- Review comments addressed: `<N>`.
- Final coverage: `<P>` %.

Closing tracker; the `implement-component` skill stops here and awaits further instructions.
