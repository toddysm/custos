<type>(<scope>): <PREFIX>-<NNN> <imperative short summary, lower-case, no period>

<one-paragraph "what + why" — focus on the user-visible or architectural
change, not the line-by-line diff>

<optional second paragraph: trade-offs, follow-ups, things explicitly out
of scope>

Changed files (key ones only):
- <path/to/file1> — <what changed>
- <path/to/file2> — <what changed>

Quality:
- ruff check / ruff format / mypy --strict all clean.
- Full suite: <N> passed, <P> % coverage.

Closes #<NNN>
